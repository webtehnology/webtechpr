'use strict';

Object.defineProperty(exports, "__esModule", {
    value: true
});

var _postcss = require('postcss');

var _postcss2 = _interopRequireDefault(_postcss);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

var modDelim = process.env.REBEM_MOD_DELIM || '_';
var elemDelim = process.env.REBEM_ELEM_DELIM || '__';

exports.default = _postcss2.default.plugin('rebem-css', function () {
    return function (css) {
        css.walkRules(function (rule) {
            rule.selector = rule.selector
            // :block(block) → .block
            .replace(/:block\(([\w-]+)\)/g, '.$1')
            // :elem(elem) → __elem
            .replace(/:elem\(([\w-]+)\)/g, elemDelim + '$1')
            // :mod(mod) → _mod
            // :mod(mod val) → _mod_val
            .replace(/:mod\(([\w-]+)\s?([\w-]+)?\)/g, function (match, mod, val) {
                if (val) {
                    return modDelim + mod + modDelim + val;
                }

                return modDelim + mod;
            });
        });
    };
});
module.exports = exports['default'];